name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - jupyter=1.0.0
  - python=3.7.3
  - pip:
    - distro==1.4.0
    - pdf-statement-reader==0.1.3
    - pikepdf==1.6.0
    - selenium==3.141.0
prefix: /anaconda3

